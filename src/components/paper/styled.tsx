import styled from 'styled-components';

export const PaperContainer = styled.div`
    background: white;
    box-shadow: 0 4px 6px rgba(50,50,93,.11), 0 1px 3px rgba(0,0,0,.08);
    border-radius: 12px;
    padding: 35px 68px;
    position: relative;
`;

export const ImgClouseSC = styled.div`
    position: absolute;
    top: 22px;
    right: 22px;
    width: 20px;
    height: 20px;
`;
